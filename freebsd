FreeBSD 10.2

Connect to the Ethernet network:
  ~#: ifconfig -a
  ~#: dhclient <name>

Package manager install (in 10.2 release pkg_add don't support):
  ~#: pkg

GHOME 3, Nano, and Xorg installation:
  ~#: pkg install nano xorg gnome3
  ~#: nano /etc/rc.conf
      dbus_enable="YES"
      hald_enable="YES"
      gdm_enable="YES"
      gnome_enable="YES"
      save file
  ~#: reboot
